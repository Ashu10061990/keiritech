/**
 * Scaffold placeholder. The real home page — six sections, ported verbatim from
 * the legacy `index.html` — lands in Task 8. Do not treat this as the design.
 */
export default function HomePage() {
  return (
    <main className="mx-auto max-w-content px-6 py-24">
      <p className="text-sm uppercase tracking-widest text-gold">Keiri Tech</p>
      <h1 className="mt-4 text-4xl font-semibold text-navy">
        AI that runs the financial back office end to end.
      </h1>
      <p className="mt-4 max-w-narrow text-muted">
        Scaffold only — page migration is Task 8 of the Phase 2 plan.
      </p>
    </main>
  );
}
