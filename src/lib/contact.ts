/**
 * Contact details, ported from KEIRITECH-INVENTORY.md §6.
 *
 * Lives in `lib/` rather than `server/` because client components need them
 * (the mailto: fallback, the footer, the JSON-LD). Nothing here is secret — it
 * is the same information printed on every page of the current site.
 */
export const CONTACT_EMAIL = "business@keiritech.com";
export const CONTACT_PHONE = "+91 8954841762";
